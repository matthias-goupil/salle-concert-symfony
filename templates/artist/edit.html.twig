{% extends 'base.html.twig' %}

{% block title %}Edit Artist{% endblock %}

{% block body %}
    {% if is_granted('ROLE_ADMIN')%}

    <div class="admin-bar">
        <a href="{{ path('app_artist_index') }}">Liste des artistes</a>
        <a href="{{ path('app_artist_show', {'id': artist.id}) }}">montrer</a>
        {{ include('artist/_delete_form.html.twig') }}
    </div>
    {% endif %}
    <h1>Modifier l'artiste</h1>

    {{ include('artist/_form.html.twig', {'button_label': 'Update'}) }}
{% endblock %}
